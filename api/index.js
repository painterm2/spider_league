// Vercel entry point: every /api/* request is rewritten here (see vercel.json)
// and handled by the same Express app that `npm start` runs locally.
import app from "../server.js";

export default app;
